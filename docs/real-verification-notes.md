Real endpoint verification update (2026-08-13): anonymous request to https://vip.prexzyapis.com/v1/models failed at TLS with OpenSSL SSL_ERROR_SYSCALL; no authenticated retry was attempted because this was a transport failure, not HTTP 401/403. No model catalog or completion request was sent. The new detector performs anonymous-first documented probes, retries only on 401/403 when an API key is supplied, and can send at most one completion request after model discovery. The regression fixture currently observes four GET discovery calls because the detector probes multiple documented model/spec paths; one POST completion remains the bounded maximum.

Latest bounded live audit (2026-08-13): DNS resolved vip.prexzyapis.com to 202.6.204.69. The legitimacy audit observed anonymous `/v1/models` HTTP 401, but authenticated `/v1/models` and invalid-credential probes ended in network errors/timeouts; TLS evidence failed with ECONNRESET. A single direct POST to `/v1/chat/completions` for the previously supplied `claude-fable-5` model also ended in an SSL connection timeout, with no HTTP status and no JSON body. Therefore no model was confirmed working, and no tool-call, streaming, vision, canary, or authenticity capability test could legitimately run against this endpoint. No response bodies or credentials were stored.
